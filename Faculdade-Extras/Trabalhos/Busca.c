#include <stdlib.h>
#include <stdio.h>
#define tam 3

typedef struct node {
    int matriz[tam][tam];
    char movimento;
    struct node *pai;
}No;

No* criaMatriz(matriz[tam][tam], char movimento, No* pai) {
    
}